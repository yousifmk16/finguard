import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function PoliciesPage() {
  return (
    <PagePlaceholder
      title="Policies"
      taskId="UI-09"
      description="Admin-only policy management. Visibility controlled by RBAC route guard."
    />
  );
}
