# Docker Compose (Sprint 0 Skeleton)

Run from this directory:

```bash
docker compose up --build
```

Services included:

- backend (FastAPI skeleton)
- frontend (Node placeholder)
- postgres
- redis
- broker (Redpanda)
