SKL-01 Deliverable
Task: Create repository and default branch rules
Repository Name: finguard
Status: Done
Owner: M5
Verified by: M1
Date: [Fill Date]

1) Repository Baseline
- Repository created: finguard
- Team members added as collaborators
- Primary branch selected: main

2) Branching Strategy (Agile-Friendly)
- `main`: production-ready, protected
- `develop`: integration branch for sprint work (optional but recommended)
- Feature branches: short-lived, one user story per branch
  - naming format: `feature/<story-id>-<short-name>`
- Bug fix branches:
  - naming format: `fix/<ticket-id>-<short-name>`
- Chore/docs/test branches:
  - `chore/<name>`, `docs/<name>`, `test/<name>`

3) Required Branch Protection Rules (main)
- No direct push to `main`
- Pull request required before merge
- Minimum 1 reviewer approval required
- All conversations must be resolved before merge
- Required status checks must pass before merge:
  - `lint`
  - `unit-tests`
  - `integration-tests` (when pipeline is ready)
- Dismiss stale approvals when new commits are pushed
- Restrict force pushes and branch deletions on `main`

4) Recommended Rules (develop)
- Pull request required before merge
- Required checks: `lint`, `unit-tests`
- At least 1 review for cross-team visibility

5) Pull Request Policy
- PR title format: `[Story-ID] Short description`
- PR must include:
  - linked user story/task
  - summary of changes
  - test evidence
  - screenshots for UI changes (if any)

6) Merge Policy
- Preferred merge: Squash and merge (clean history)
- Merge only when DoD is satisfied:
  - code complete
  - review complete
  - tests passing
  - docs updated

7) Definition of Done Alignment
- Every merged PR should satisfy sprint DoD and be demo-ready.

8) SKL-01 Acceptance Checklist
- [x] Repository `finguard` exists
- [x] Team collaborators added
- [x] Default branch selected
- [x] Branch protection rules defined
- [x] PR/merge policy documented

9) Next Immediate Tasks
- SKL-02: Create folder skeleton in repo
- SKL-14: Add CI pipeline checks referenced by branch rules
- SKL-15: Add PR template and issue templates
