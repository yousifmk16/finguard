import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      taskId="UI-06"
      description="KPI cards, trend widgets, and active anomalies summary land here."
    />
  );
}
